# Replit injects REPLIT_DB_URL automatically inside any Repl — nothing to set.
# Only override it here if you're developing locally outside of Replit and
# want to point at a specific Replit DB endpoint.
# REPLIT_DB_URL=
